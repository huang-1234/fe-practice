import { BaseService } from '../../base/BaseService'

export class UpdateDashboardEnable extends BaseService {
  async execute(dashboard: Dashbord, isEnable: number) {
    throw new Error('not implement')
  }
}